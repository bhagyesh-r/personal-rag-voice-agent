# personal-rag-voice-agent

Express + TypeScript starter API.

## Endpoints

- `GET /hello` → `{ "message": "Hello, world!" }`

## Scripts

- `npm run dev` - run in watch mode using tsx
- `npm run build` - compile TypeScript to `dist/`
- `npm start` - run compiled app
